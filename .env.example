# Supabase project credentials (free tier is sufficient).
# Create a project at https://supabase.com, then copy these from
# Project Settings -> API. Realtime is enabled by default; no tables required.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
